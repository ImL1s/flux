name: flux_dap
description: Debug Adapter Protocol implementation for Flux language
version: 2.0.0

environment:
  sdk: ^3.6.0

dependencies:
  flux_vm: ^2.0.0
  flux_compiler: ^2.0.0
  stream_channel: ^2.1.0
  args: ^2.4.0

dev_dependencies:
  test: ^1.24.0
  lints: ^6.0.0
