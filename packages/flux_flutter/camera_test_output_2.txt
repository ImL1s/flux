00:00 +0: loading D:/OtherProject/mine/flux/packages/flux_flutter/test/modules/camera_module_test.dart
00:00 +0: CameraModule availableCameras returns mapped data
00:00 +0 -1: CameraModule availableCameras returns mapped data [E]
  Bad state: A test tried to use `any` or `captureAny` on a parameter of type `CameraDescription`, but
  registerFallbackValue was not previously called to register a fallback value for `CameraDescription`.
  
  To fix, do:
  
  ```
  void main() {
    setUpAll(() {
      registerFallbackValue(/* create a dummy instance of `CameraDescription` */);
    });
  }
  ```
  
  This instance of `CameraDescription` will only be passed around, but never be interacted with.
  Therefore, if `CameraDescription` is a function, it does not have to return a valid object and
  could throw unconditionally.
  If you cannot easily create an instance of `CameraDescription`, consider defining a `Fake`:
  
  ```
  class MyTypeFake extends Fake implements MyType {}
  
  void main() {
    setUpAll(() {
      registerFallbackValue(MyTypeFake());
    });
  }
  ```
  
  Fallbacks are required because mocktail has to know of a valid `CameraDescription` to prevent
  TypeErrors from being thrown in Dart's sound null safe mode, while still
  providing a convenient syntax.
  
  package:mocktail/src/_register_matcher.dart 49:5    _getFallbackValue
  package:mocktail/src/_register_matcher.dart 160:25  _registerMatcher
  package:mocktail/src/_register_matcher.dart 104:10  any
  test\modules\camera_module_test.dart 48:47          main.<fn>.<fn>.<fn>
  package:mocktail/src/mocktail.dart 211:9            when.<fn>
  test\modules\camera_module_test.dart 48:11          main.<fn>.<fn>
  
00:00 +0 -1: CameraModule initialize creates and initializes controller
00:00 +0 -2: CameraModule initialize creates and initializes controller [E]
  Bad state: A test tried to use `any` or `captureAny` on a parameter of type `CameraDescription`, but
  registerFallbackValue was not previously called to register a fallback value for `CameraDescription`.
  
  To fix, do:
  
  ```
  void main() {
    setUpAll(() {
      registerFallbackValue(/* create a dummy instance of `CameraDescription` */);
    });
  }
  ```
  
  This instance of `CameraDescription` will only be passed around, but never be interacted with.
  Therefore, if `CameraDescription` is a function, it does not have to return a valid object and
  could throw unconditionally.
  If you cannot easily create an instance of `CameraDescription`, consider defining a `Fake`:
  
  ```
  class MyTypeFake extends Fake implements MyType {}
  
  void main() {
    setUpAll(() {
      registerFallbackValue(MyTypeFake());
    });
  }
  ```
  
  Fallbacks are required because mocktail has to know of a valid `CameraDescription` to prevent
  TypeErrors from being thrown in Dart's sound null safe mode, while still
  providing a convenient syntax.
  
  package:mocktail/src/_register_matcher.dart 49:5    _getFallbackValue
  package:mocktail/src/_register_matcher.dart 160:25  _registerMatcher
  package:mocktail/src/_register_matcher.dart 104:10  any
  test\modules\camera_module_test.dart 48:47          main.<fn>.<fn>.<fn>
  package:mocktail/src/mocktail.dart 211:9            when.<fn>
  test\modules\camera_module_test.dart 48:11          main.<fn>.<fn>
  
00:00 +0 -2: CameraModule takePicture returns path on success
00:00 +0 -3: CameraModule takePicture returns path on success [E]
  Bad state: A test tried to use `any` or `captureAny` on a parameter of type `CameraDescription`, but
  registerFallbackValue was not previously called to register a fallback value for `CameraDescription`.
  
  To fix, do:
  
  ```
  void main() {
    setUpAll(() {
      registerFallbackValue(/* create a dummy instance of `CameraDescription` */);
    });
  }
  ```
  
  This instance of `CameraDescription` will only be passed around, but never be interacted with.
  Therefore, if `CameraDescription` is a function, it does not have to return a valid object and
  could throw unconditionally.
  If you cannot easily create an instance of `CameraDescription`, consider defining a `Fake`:
  
  ```
  class MyTypeFake extends Fake implements MyType {}
  
  void main() {
    setUpAll(() {
      registerFallbackValue(MyTypeFake());
    });
  }
  ```
  
  Fallbacks are required because mocktail has to know of a valid `CameraDescription` to prevent
  TypeErrors from being thrown in Dart's sound null safe mode, while still
  providing a convenient syntax.
  
  package:mocktail/src/_register_matcher.dart 49:5    _getFallbackValue
  package:mocktail/src/_register_matcher.dart 160:25  _registerMatcher
  package:mocktail/src/_register_matcher.dart 104:10  any
  test\modules\camera_module_test.dart 48:47          main.<fn>.<fn>.<fn>
  package:mocktail/src/mocktail.dart 211:9            when.<fn>
  test\modules\camera_module_test.dart 48:11          main.<fn>.<fn>
  
00:00 +0 -3: CameraModule Video Recording startVideoRecording calls controller
00:00 +0 -4: CameraModule Video Recording startVideoRecording calls controller [E]
  Bad state: A test tried to use `any` or `captureAny` on a parameter of type `CameraDescription`, but
  registerFallbackValue was not previously called to register a fallback value for `CameraDescription`.
  
  To fix, do:
  
  ```
  void main() {
    setUpAll(() {
      registerFallbackValue(/* create a dummy instance of `CameraDescription` */);
    });
  }
  ```
  
  This instance of `CameraDescription` will only be passed around, but never be interacted with.
  Therefore, if `CameraDescription` is a function, it does not have to return a valid object and
  could throw unconditionally.
  If you cannot easily create an instance of `CameraDescription`, consider defining a `Fake`:
  
  ```
  class MyTypeFake extends Fake implements MyType {}
  
  void main() {
    setUpAll(() {
      registerFallbackValue(MyTypeFake());
    });
  }
  ```
  
  Fallbacks are required because mocktail has to know of a valid `CameraDescription` to prevent
  TypeErrors from being thrown in Dart's sound null safe mode, while still
  providing a convenient syntax.
  
  package:mocktail/src/_register_matcher.dart 49:5    _getFallbackValue
  package:mocktail/src/_register_matcher.dart 160:25  _registerMatcher
  package:mocktail/src/_register_matcher.dart 104:10  any
  test\modules\camera_module_test.dart 48:47          main.<fn>.<fn>.<fn>
  package:mocktail/src/mocktail.dart 211:9            when.<fn>
  test\modules\camera_module_test.dart 48:11          main.<fn>.<fn>
  
00:00 +0 -4: CameraModule Video Recording stopVideoRecording returns video file
00:00 +0 -5: CameraModule Video Recording stopVideoRecording returns video file [E]
  Bad state: A test tried to use `any` or `captureAny` on a parameter of type `CameraDescription`, but
  registerFallbackValue was not previously called to register a fallback value for `CameraDescription`.
  
  To fix, do:
  
  ```
  void main() {
    setUpAll(() {
      registerFallbackValue(/* create a dummy instance of `CameraDescription` */);
    });
  }
  ```
  
  This instance of `CameraDescription` will only be passed around, but never be interacted with.
  Therefore, if `CameraDescription` is a function, it does not have to return a valid object and
  could throw unconditionally.
  If you cannot easily create an instance of `CameraDescription`, consider defining a `Fake`:
  
  ```
  class MyTypeFake extends Fake implements MyType {}
  
  void main() {
    setUpAll(() {
      registerFallbackValue(MyTypeFake());
    });
  }
  ```
  
  Fallbacks are required because mocktail has to know of a valid `CameraDescription` to prevent
  TypeErrors from being thrown in Dart's sound null safe mode, while still
  providing a convenient syntax.
  
  package:mocktail/src/_register_matcher.dart 49:5    _getFallbackValue
  package:mocktail/src/_register_matcher.dart 160:25  _registerMatcher
  package:mocktail/src/_register_matcher.dart 104:10  any
  test\modules\camera_module_test.dart 48:47          main.<fn>.<fn>.<fn>
  package:mocktail/src/mocktail.dart 211:9            when.<fn>
  test\modules\camera_module_test.dart 48:11          main.<fn>.<fn>
  
00:00 +0 -5: CameraModule setFlashMode mapping
00:00 +0 -6: CameraModule setFlashMode mapping [E]
  Bad state: A test tried to use `any` or `captureAny` on a parameter of type `CameraDescription`, but
  registerFallbackValue was not previously called to register a fallback value for `CameraDescription`.
  
  To fix, do:
  
  ```
  void main() {
    setUpAll(() {
      registerFallbackValue(/* create a dummy instance of `CameraDescription` */);
    });
  }
  ```
  
  This instance of `CameraDescription` will only be passed around, but never be interacted with.
  Therefore, if `CameraDescription` is a function, it does not have to return a valid object and
  could throw unconditionally.
  If you cannot easily create an instance of `CameraDescription`, consider defining a `Fake`:
  
  ```
  class MyTypeFake extends Fake implements MyType {}
  
  void main() {
    setUpAll(() {
      registerFallbackValue(MyTypeFake());
    });
  }
  ```
  
  Fallbacks are required because mocktail has to know of a valid `CameraDescription` to prevent
  TypeErrors from being thrown in Dart's sound null safe mode, while still
  providing a convenient syntax.
  
  package:mocktail/src/_register_matcher.dart 49:5    _getFallbackValue
  package:mocktail/src/_register_matcher.dart 160:25  _registerMatcher
  package:mocktail/src/_register_matcher.dart 104:10  any
  test\modules\camera_module_test.dart 48:47          main.<fn>.<fn>.<fn>
  package:mocktail/src/mocktail.dart 211:9            when.<fn>
  test\modules\camera_module_test.dart 48:11          main.<fn>.<fn>
  
00:00 +0 -6: CameraModule dispose cleans up controller
00:00 +0 -7: CameraModule dispose cleans up controller [E]
  Bad state: A test tried to use `any` or `captureAny` on a parameter of type `CameraDescription`, but
  registerFallbackValue was not previously called to register a fallback value for `CameraDescription`.
  
  To fix, do:
  
  ```
  void main() {
    setUpAll(() {
      registerFallbackValue(/* create a dummy instance of `CameraDescription` */);
    });
  }
  ```
  
  This instance of `CameraDescription` will only be passed around, but never be interacted with.
  Therefore, if `CameraDescription` is a function, it does not have to return a valid object and
  could throw unconditionally.
  If you cannot easily create an instance of `CameraDescription`, consider defining a `Fake`:
  
  ```
  class MyTypeFake extends Fake implements MyType {}
  
  void main() {
    setUpAll(() {
      registerFallbackValue(MyTypeFake());
    });
  }
  ```
  
  Fallbacks are required because mocktail has to know of a valid `CameraDescription` to prevent
  TypeErrors from being thrown in Dart's sound null safe mode, while still
  providing a convenient syntax.
  
  package:mocktail/src/_register_matcher.dart 49:5    _getFallbackValue
  package:mocktail/src/_register_matcher.dart 160:25  _registerMatcher
  package:mocktail/src/_register_matcher.dart 104:10  any
  test\modules\camera_module_test.dart 48:47          main.<fn>.<fn>.<fn>
  package:mocktail/src/mocktail.dart 211:9            when.<fn>
  test\modules\camera_module_test.dart 48:11          main.<fn>.<fn>
  
00:00 +0 -7: Some tests failed.
