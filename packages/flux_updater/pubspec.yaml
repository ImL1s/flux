name: flux_updater
description: OTA update system for Flux with bytecode diff and version control.
version: 1.0.0
homepage: https://github.com/ImL1s/flux
repository: https://github.com/ImL1s/flux

environment:
  sdk: ^3.6.0

dependencies:
  flux_compiler:
    path: ../flux_compiler
  crypto: ^3.0.3
  path: ^1.9.0
  archive: ^4.0.7
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  http: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
