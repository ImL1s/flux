name: showcase_app
description: Flux 綜合展示應用 - 展示 Flux 腳本在真實場景中的應用
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  flux_flutter:
    path: ../../../packages/flux_flutter
  flux_vm:
    path: ../../../packages/flux_vm
  flutter_riverpod: ^3.0.1
  dio: ^5.7.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
